
<h1>Hello I am on the petition Page</h1>
<form method="POST">
    {{!-- <input name="first_name"/>
    <input name="last_name"/> --}}
    <input name="signature" type="hidden"/>
    <input type="hidden" name="_csrf" value="{{csrfToken}}">

    <canvas id="canvas" width="400" height="400"></canvas>
    <button id="petitionButton" type="submit">Submit</button>
</form>
<a href="/logout">Logout? Click here</a>

 {{#if noSignature}}
        {{>noSignature}}
    {{/if}}

 {{!-- <input name="signature" type="hidden"/> --}}